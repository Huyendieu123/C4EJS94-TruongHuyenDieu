for (let item of array) {
    console.log(item);
}
//  in ra các giá trị trong array, chỉ dùng cho array

for (let item in array) {
    console.log(array[item]);
}

//in ra giá trị của vị trí item trong array, dùng cho cả array, object

// include : hàm bao gồm, tìm xem trong array....