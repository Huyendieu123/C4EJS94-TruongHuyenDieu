alert('Can a variable declared by let, in a function still exists after the function finishes running');
alert('a variable declared by let, in a function does not exists after the function finishes running');
alert ('Can var variable declared by var, in a function still exists after the function finishes running');
alert('Yes');
